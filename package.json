{
  "scripts": {
    "build": "tsc -p .",
    "build:watch": "tsc -p . -w",
    "clean": "rimraf src/main/resources"
  },
  "dependencies": {
    "fs-extra": "^9.0.0"
  },
  "devDependencies": {
    "@types/fs-extra": "9.0.0",
    "@types/node": "^14.0.1",
    "typescript": "3.9.2",
    "rimraf": "^2.7.0"
  }
}
